# Node / Bun
node_modules/
*.tsbuildinfo
.DS_Store

# Tooling scratch
.playwright-mcp/
*.png

# Local capture artifacts — never commit captured activity
*.db
*.db-wal
*.db-shm
postcaptain.db

# Local redaction denylist — environment-specific company domains/repos
redaction.toml

# Local HMAC salt for redaction pseudonyms — must never leave the machine
.postcaptain.salt
*.salt
