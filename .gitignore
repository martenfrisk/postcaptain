# Node / Bun
node_modules/
*.tsbuildinfo
.DS_Store

# Local capture artifacts — never commit captured activity
*.db
*.db-wal
*.db-shm
postcaptain.db

# Local redaction denylist — environment-specific company domains/repos
redaction.toml
